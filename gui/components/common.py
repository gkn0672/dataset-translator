def create_heading(text, color="#ff7f00"):
    """
    Create a styled heading HTML element.

    Args:
        text (str): Heading text
        color (str): Heading color in hex

    Returns:
        str: HTML for the heading
    """
    return f"{text}"


def create_field_label(text):
    """
    Create a styled field label HTML element.

    Args:
        text (str): Label text

    Returns:
        str: HTML for the field label
    """
    return f"{text}"
